import styled from 'styled-components'


export const Container = styled.div`
    position: fixed;
    right: 20px;
    bottom: 20px;

    a{
        text-decoration: none;
        cursor: pointer;
        margin: 6px;
        color: var(--white);

        :hover{
            color: var(--green);
        }
    }

`
